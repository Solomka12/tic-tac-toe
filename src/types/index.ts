export enum PlayerSign {
  X = 'x',
  O = 'o'
}

export enum GameMode {
  Single = 'Single',
  Multi = 'Multi',
  Ai = 'Ai'
}

export enum Difficulty {
  Easy = 'easy',
  Medium = 'medium',
  Hard = 'hard',
}
